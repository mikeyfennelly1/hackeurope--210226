info:
  name: 03 - send true for inputNode-1771733818889
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/redprints/{{redprintId}}/nodes/inputNode-1771733818889"
  body:
    type: json
    data: |-
      {
        "output": true
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const baseUrl = bru.getEnvVar("baseUrl");

        const { data: redprints } = await axios.get(`${baseUrl}/api/redprints`);

        const running = redprints.find(r => r.status === "running");

        if (!running) {
          throw new Error("No running redprint found — dispatch one first");
        }

        bru.setVar("redprintId", running.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
