info:
  name: 01 - Get Redprints
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/redprints"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
